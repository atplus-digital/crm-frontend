# Comprehensive Code Review — CRM AT+

**Date:** 2026-05-06
**Scope:** Full `src/` directory
**Method:** 5 parallel agents (duplication, errors, architecture, performance, code quality)
**Baseline:** Typecheck ✅ passes clean · Biome lint ✅ passes clean · Zero `as any` · Zero `@ts-ignore`

---

## Overall Assessment: 7.8/10

The codebase is well-structured and disciplined. Type safety, linting, and error handling foundations are solid. Main issues: **pattern duplication across CS subdomains** (~1,300 lines) and **missing per-route error/suspense boundaries**.

---

## 🔴 Critical Issues

### 1. No Per-Route Error Boundaries — One Bug Crashes Everything

`src/app.tsx` has a single `ErrorBoundary` wrapping the root `<Outlet />`. A render error in any tab takes down the entire application (sidebar, navigation, everything).

**Files:** `src/app.tsx:21-27`, `src/routes/router.tsx`
**Fix:** Wrap each feature route outlet and tab content area with `<ErrorBoundary>`.

- [x] **Executar:** Adicionar `<ErrorBoundary>` por rota/aba — isolar falhas

---

### 2. Missing `<Suspense>` Boundaries for Lazy Routes

All routes use `lazy()` for code splitting, but **zero `<Suspense>` boundaries exist**. React Router's `lazy()` REQUIRES Suspense to catch the thrown promise during loading. Users see blank screens on route transitions.

**Files:** `src/app.tsx`, `src/routes/router.tsx`
**Fix:** Wrap `<Outlet />` in `<Suspense fallback={<LoadingSpinner />}>`.

- [x] **Executar:** Adicionar `<Suspense>` no `app.tsx` em volta do `<Outlet />`

---

### 3. Unsafe `(error as Error).message` in 14+ Pages

TanStack Query errors from NocoBase/IXC may NOT be `Error` instances (can be plain objects or strings). Casting them as `Error` returns `undefined` silently when the error isn't an Error instance.

**Files:** Every page-level component displaying `{(error as Error).message}` (14+ locations)
**Fix:** Create `getErrorMessage(error: unknown): string` utility and use consistently.

- [x] **Executar:** Criar `getErrorMessage()` em `src/lib/` e substituir em todas as páginas

---

## 🟠 High Severity

### 4. Massive Code Duplication (~1,323 lines)

| Duplication                                                  | Files       | Lines |
| ------------------------------------------------------------ | ----------- | ----- |
| List components (PaginationInfo + sort + DataTableContainer) | 7           | ~300  |
| Query hooks (queryOptions + staleTime + useXxxById)          | 7           | ~160  |
| Filter bars (FilterLayout + FilterActions + flush)           | 5           | ~475  |
| Service error handling (try/catch + log + rethrow)           | 4           | ~108  |
| List page composition (useListPage + PageLayout + query)     | 3           | ~180  |
| Status filter options (Object.entries().map())               | 9 instances | ~72   |
| Response meta normalization (repos)                          | 2           | ~20   |

- [x] **Executar 4a:** Extrair `createNocoBaseListHook` factory (elimina duplicação de query hooks + list pages)
- [x] **Executar 4b:** Extrair `useTableSort` hook + `PaginatedListPage` wrapper (elimina duplicação de list components)
- [x] **Executar 4c:** Extrair `FilterBarShell` component (elimina duplicação de filter bars)
- [x] **Executar 4d:** Criar `withServiceErrorHandling` wrapper (elimina duplicação de error handling em services)
- [x] **Executar 4e:** Extrair `toFilterOptions<T>()` utility (elimina 9 `Object.entries().map()` idênticos)
- [x] **Executar 4f:** Unificar `normalizePaginatedResponse` entre `nocobase-repository` e `ixc-repository`

---

### 5. Inconsistent Data Access Patterns

CS subdomains use **two different patterns:**

| Pattern A: Service Layer        | Pattern B: Direct Repository                                 |
| ------------------------------- | ------------------------------------------------------------ |
| contratos, negociacoes, pessoas | troca-titularidade, suspensao-de-contrato, troca-de-endereco |
| hooks → service → repository    | hooks → repository (no service file)                         |

Pattern B subdomains bypass type safety: `"t_troca_endereco" as "users"` and double `as unknown as` casts.

- [x] **Executar:** Unificar para Pattern A (service layer) ou documentar Pattern B e aplicar consistência

---

### 6. Unsafe Type Assertions in Kanban Dashboard

`kanban-dashboard-hooks.ts` has 5 type assertion bypasses (`as "users"`, `as unknown as`) because `nocobaseRepository.list()` doesn't accept all collection names.

**Fix:** Add missing collections to the typed client overloads or use generic string parameter.

- [x] **Executar:** Registrar coleções (`t_troca_endereco`, `t_suspensao_contrato`) no typed client do repositório

---

### 7. KanbanBoard: No Memoization on Data-Heavy Views

- `kanban-board.tsx`: sorts and groups up to 800 cards on every render without `useMemo`
- `kanban-card.tsx`: not wrapped in `React.memo` despite hundreds of instances
- `kanban-dashboard-hooks.ts`: normalizes arrays on every render

- [x] **Executar 7a:** `useMemo` para `sortCardsByCreatedAtDesc` + `groupCardsByStatus` em `kanban-board.tsx`
- [x] **Executar 7b:** `React.memo` no `KanbanCard` com `useCallback` nos handlers
- [x] **Executar 7c:** Memoizar normalização de cards em `kanban-dashboard-hooks.ts`

---

## 🟡 Medium Severity

### 8. Empty Catch Block in use-custom-requests.ts

`resolveCurrentUserData` (line 127) silently swallows errors from `nocobaseRepository.get("users", ...)`. No logging occurs — API failures are invisible.

- [x] **Executar:** Adicionar `log.warn("Failed to fetch full user data, using auth store fallback")` no catch
- Adicionar toast para feedback do usuário

---

### 9. Unsafe localStorage Cast in Theme Provider

`theme-provider.tsx:32`: `localStorage.getItem(storageKey) as Theme` — any corrupted or tampered value passes through unchecked.

- [x] **Executar:** Adicionar type guard antes do cast: `if (stored === "dark" || stored === "light" || stored === "system") return stored`

---

### 10. Large Unsplit Files

| File                        | Lines | Issue                                              |
| --------------------------- | ----- | -------------------------------------------------- |
| `kanban-dashboard-hooks.ts` | 506   | 4 query functions + normalizers + sort             |
| `kanban-dashboard-types.ts` | 395   | Types + badge configs + status maps + sort options |
| `negociacoes-service.ts`    | 353   | Filter builder + CRUD + CSV export                 |

- [x] **Executar 10a:** Quebrar `kanban-dashboard-hooks.ts` (506 lines) — extrair query factory
- [x] **Executar 10b:** Quebrar `kanban-dashboard-types.ts` (395 lines) — separar tipos, configs, e maps
- [x] **Executar 10c:** Quebrar `negociacoes-service.ts` (353 lines) — extrair filter builder

---

### 11. Barrel Export Violations

| Barrel File                                       | Issue                                             |
| ------------------------------------------------- | ------------------------------------------------- |
| `custom-requests/components/index.ts`             | Exporta só 1 item (`PopupRequest`)                |
| `vendas-filters/index.ts`                         | Exporta só 1 item (`VendasFilters`)               |
| `negociacao-detalhes-tab/index.ts`                | Exporta só 1 item (`NegociacaoDetalhesTab`)       |
| `troca-titularidade/components/sections/index.ts` | Exporta 7 componentes internos (vazamento de API) |

- [x] **Executar:** Remover barrels de 1 item e o barrel de sections (componentes internos)

---

### 12. Debug Page Committed to Production

`src/pages/testes/testes.tsx` — página de debug/dev sem gate `isDev`.

- [x] **Executar:** Remover página ou gate atrás de `isDev`

---

### 13. Trivial Type Aliases That Add No Value

`contratos-types.ts` defines 7 aliases: `type Contrato = ClienteContrato`, `type Fatura = FnAreceber`, etc. Consumidores deveriam importar direto de `#/generated/`.

- [x] **Executar:** Remover aliases triviais e importar direto dos tipos gerados

---

### 14. Missing AGENTS.md

| Directory                     | Status                                       |
| ----------------------------- | -------------------------------------------- |
| `src/features/cs/components/` | Tem 3 arquivos compartilhados, sem AGENTS.md |
| `src/pages/cs/propostas/`     | Sem AGENTS.md                                |

- [x] **Executar:** Criar AGENTS.md nos diretórios faltantes

---

## 🟢 Low Severity

### 15. `console.error` in Production Code

`use-popup-request.ts:66,76` usa `console.error` em vez de `createLogger()` do projeto.

- [x] **Executar:** Substituir por `createLogger()`

---

### 16. Relative Import Paths (9 occurrences)

`custom-requests/` e `negociacoes/utils/` usam `../../` em vez do alias `#/`.

- [x] **Executar:** Migrar imports relativos para `#/` alias

---

### 17. `useMemo` Misused for Side Effects

`filter-input-field.tsx:79` atribui `flushRef.current` dentro de `useMemo` — deveria ser `useEffect` ou atribuição direta.

- [x] **Executar:** Corrigir `useMemo` → `useEffect` ou atribuição direta

---

### 18. Dynamic Tailwind Classes May Not Compile

`negociacao-anexos-grid.tsx` usa `` `sm:grid-cols-${columns.sm}` `` que o compilador Tailwind v4 pode não detectar.

- [x] **Executar:** Utilizar funçao cn

---

### 19. `text-[11px]` Instead of Canonical Class

`popup-error-content.tsx:44,47` usa `text-[11px]` em vez de `text-xs` ou CSS variable.

- [x] **Executar:** Substituir por classe canônica

---

### 20. `max-w-400` Repeated Across 6 Files

Seis páginas de detalhe duplicam `mx-auto max-w-400 p-4`.

- [x] **Executar:** Extrair para componente `DetailPageContainer` ou usar `PageLayout` consistente

---

### 21. Service Functions Re-Exported from Hooks

`negociacoes-hooks.ts` e `pessoas-hooks.ts` re-exportam funções de service. Hooks deveriam exportar apenas hooks.

- [x] **Executar:** Remover re-exports de service dos arquivos de hooks

---

## ✅ What's Already Good

- Zero `as any`, `@ts-ignore`, empty catch blocks
- Typecheck + lint pass clean (1006 files)
- Service-layer error handling (try/catch/log/rethrow — just duplicated)
- React Query usage with `staleTime`, parallel `useQueries`
- Lazy route loading with `lazy()`
- Auth validation with Zod `safeParse`
- Loading states on all pages
- No lodash — zero heavy utility deps
- Generated types are primary source (no interface duplication)
- Cross-feature imports use correct public API boundaries

---

## Priority Action Plan Summary

| #    | Action                                            | Severity    | Effort  |
| ---- | ------------------------------------------------- | ----------- | ------- |
| 1    | Add `<Suspense>` + per-route `<ErrorBoundary>`    | 🔴 Critical | Baixo   |
| 2    | Create `getErrorMessage()` utility                | 🔴 Critical | Baixo   |
| 3    | Add logging to empty catch in use-custom-requests | 🟠 High     | Trivial |
| 4a   | Extract `createNocoBaseListHook` factory          | 🟠 High     | Médio   |
| 4b   | Extract `useTableSort` + shared PaginationInfo    | 🟠 High     | Baixo   |
| 4c   | Extract `FilterBarShell` component                | 🟠 High     | Médio   |
| 5    | Unify data access patterns (service layer)        | 🟠 High     | Médio   |
| 6    | Fix collection type overrides                     | 🟠 High     | Médio   |
| 7a-c | Memoize KanbanBoard computations                  | 🟠 High     | Médio   |
| 8    | Validate localStorage theme value                 | 🟡 Medium   | Trivial |
| 9    | Remove single-item barrels + sections/index.ts    | 🟡 Medium   | Trivial |
| 10   | Remove debug page or gate behind isDev            | 🟡 Medium   | Trivial |
| 11   | Replace console.error with createLogger           | 🟢 Low      | Trivial |
| 12   | Migrate relative imports to #/ alias              | 🟢 Low      | Trivial |
| 13   | Fix useMemo side effect in filter-input-field     | 🟢 Low      | Trivial |
| 14   | Fix dynamic Tailwind classes                      | 🟢 Low      | Trivial |
| 15   | Extract toFilterOptions utility                   | 🟢 Low      | Trivial |

**Estimated total effort:** 3-5 dias para todos os itens.
**High-priority (1-7):** 1-2 dias com trabalho paralelo.
