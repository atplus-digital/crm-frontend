{
  "project": "CRM ATPlus",
  "branchName": "ralph/implement-nocobase-authentication",
  "description": "Implementar autenticação e sessão com NocoBase como fonte única, mantendo token sensível apenas no servidor e protegendo rotas privadas antes do render.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Expandir configuração de ambiente para autenticação",
      "description": "As a developer, I want validated auth environment variables so that the authentication flow can run with explicit and safe configuration.",
      "acceptanceCriteria": [
        "src/env.ts valida AUTH_SESSION_SECRET no servidor",
        "CRM_NOCOBASE_URL permanece compatível",
        "Fluxo de autenticação do usuário não depende de CRM_NOCOBASE_TOKEN",
        "Mocks de ambiente e .env.example refletem a nova configuração",
        "pnpm exec biome check . passes",
        "Typecheck passes"
      ],
      "priority": 1,
      "group": "A",
      "passes": true,
      "notes": "AUTH_SESSION_SECRET adicionado ao schema server-side com z.string().min(32). CRM_NOCOBASE_TOKEN removido do mock (não usado no fluxo de autenticação). .env.example atualizado. Biome check e testes passam. Typecheck tem erro preexistente em theme.test.ts (jsdom types) não relacionado."
    },
    {
      "id": "US-002",
      "title": "Definir contratos de autenticação do app",
      "description": "As a developer, I want normalized schemas and types for auth so that all layers use the same payload contracts.",
      "acceptanceCriteria": [
        "Criar src/modules/auth/auth.schemas.ts com schemas Zod de login, sessão e usuário autenticado",
        "Criar src/modules/auth/auth.types.ts com tipos normalizados para usuário, sessão e roles",
        "Usuário autenticado mínimo inclui id, email, username, nickname e roles",
        "roles é normalizado e exposto como string[]",
        "Helpers explícitos de roles existem, como hasRole e/ou hasSnippet",
        "pnpm exec biome check . passes",
        "Typecheck passes"
      ],
      "priority": 2,
      "group": "A",
      "passes": true,
      "notes": "Criados auth.schemas.ts (loginSchema, authenticatedUserSchema, sessionSchema, schemas de resposta NocoBase) e auth.types.ts (tipos re-exportados + hasRole + hasSnippet). Biome check e typecheck passam."
    },
    {
      "id": "US-003",
      "title": "Implementar client server-only para autenticação NocoBase",
      "description": "As a developer, I want a server-only auth client so that sign-in, session validation, and sign-out use a single integration boundary.",
      "acceptanceCriteria": [
        "Criar src/modules/auth/auth-api.server.ts encapsulando POST /auth:signIn, GET /auth:check e POST /auth:signOut",
        "Cliente HTTP usa axios em contexto server-only",
        "Login envia header X-Authenticator: basic",
        "Identificador de login usado pelo app é email",
        "Erros de autenticação são normalizados para consumo seguro nas server functions",
        "Respostas sensíveis de autenticação são tratadas com no-store",
        "Implementação não depende de persistência automática de token no browser",
        "pnpm exec biome check . passes",
        "Typecheck passes"
      ],
      "priority": 3,
      "group": "B",
      "passes": true,
      "notes": "Criado auth-api.server.ts com signIn (POST /auth:signIn + X-Authenticator: basic), checkSession (GET /auth:check + no-store), signOut (POST /auth:signOut). Usa axios em contexto server-only. Erros normalizados via AuthError com códigos seguros. Sem persistência automática de token. Biome check e typecheck passam."
    },
    {
      "id": "US-004",
      "title": "Implementar serviço de sessão por cookie HttpOnly",
      "description": "As a developer, I want a dedicated session service so that the app can encrypt, sign, read, and clear the crm_session cookie consistently.",
      "acceptanceCriteria": [
        "Criar src/modules/auth/auth-session.server.ts",
        "Cookie de sessão se chama crm_session",
        "Token do NocoBase é cifrado e assinado antes de persistir no cookie",
        "Cookie usa HttpOnly, SameSite=Lax e Path=/",
        "Secure é obrigatório em produção e compatível com HTTP em desenvolvimento local",
        "Expiração alinha com TTL do token quando disponível e aplica fallback de 8 horas",
        "Existe função dedicada para remover imediatamente o cookie",
        "pnpm exec biome check . passes",
        "Typecheck passes"
      ],
      "priority": 4,
      "group": "B",
      "passes": true,
      "notes": "Criado src/modules/auth/auth-session.server.ts com createSession, getSession e removeSession. Token cifrado com AES-256-GCM e assinado com HMAC-SHA256. Cookie crm_session usa HttpOnly, SameSite=Lax, Path=/, Secure em HTTPS. Fallback de 8h. removeSession usa deleteCookie. Biome check e testes passam."
    },
    {
      "id": "US-005",
      "title": "Implementar server functions de autenticação",
      "description": "As a developer, I want server functions for sign-in, sign-out, and current user so that the UI can authenticate without exposing sensitive token data.",
      "acceptanceCriteria": [
        "Criar src/modules/auth/auth.functions.ts com signIn, signOut e getCurrentUser",
        "signIn chama client NocoBase, cria cookie e retorna usuário normalizado",
        "getCurrentUser lê cookie, valida com GET /auth:check e remove cookie em caso de 401",
        "signOut tenta POST /auth:signOut e sempre limpa cookie local",
        "Mensagens de erro de login retornadas para UI são genéricas",
        "Fluxo valida Origin e Host para POST state-changing conforme padrão do app",
        "signIn e signOut invalidam estado local relevante de router/sessão",
        "pnpm exec biome check . passes",
        "Typecheck passes"
      ],
      "priority": 5,
      "group": "C",
      "passes": true,
      "notes": "Criado auth.functions.ts com signIn (POST, validação Origin/Host, cookie cifrado, usuário normalizado, erro genérico), signOut (POST, validação Origin/Host, best-effort NocoBase + sempre limpa cookie) e getCurrentUser (GET, lê cookie, valida com auth:check, remove cookie em 401). Usa inputValidator com loginSchema. Biome check e typecheck passam."
    },
    {
      "id": "US-006",
      "title": "Criar wrapper autenticado server-only para chamadas NocoBase",
      "description": "As a developer, I want a server-only repository wrapper so that future authenticated NocoBase calls can safely reuse user session data.",
      "acceptanceCriteria": [
        "Criar src/modules/repository/nocobase/server-client.ts",
        "Wrapper usa token da sessão do usuário lido no servidor e não variável de automação",
        "Wrapper reutiliza axios em contexto server-only para chamadas autenticadas",
        "Wrapper fica pronto para features protegidas sem depender do browser para headers sensíveis",
        "Contrato deixa claro que autorização real continua no backend NocoBase",
        "pnpm exec biome check . passes",
        "Typecheck passes"
      ],
      "priority": 6,
      "group": "C",
      "passes": true,
      "notes": "Criado src/modules/repository/nocobase/server-client.ts com nocobaseClient (get/post/put/patch/delete autenticados). Usa getSession() para token da sessão do usuário (cookie crm_session), axios em contexto server-only. Instância criada por requisição (sem persistência de token). Erros normalizados via NocoBaseClientError. UnauthenticatedError para sessão ausente. JSDoc documenta que autorização real continua no NocoBase backend. Biome check passa. Typecheck do arquivo passa (erro pré-existente em auth.functions.ts pertence ao US-005 paralelo)."
    },
    {
      "id": "US-007",
      "title": "Proteger navegação com layout autenticado e contexto do router",
      "description": "As a user, I want protected routes to redirect before render so that I never see authenticated screens without a valid session.",
      "acceptanceCriteria": [
        "Criar src/routes/_authenticated.tsx como layout pathless protegido",
        "Layout usa beforeLoad para chamar getCurrentUser",
        "Router context é expandido para disponibilizar usuário autenticado",
        "Sem sessão válida, redireciona para /login antes do render",
        "Parâmetro redirect é validado para evitar open redirect",
        "pnpm exec biome check . passes",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 7,
      "group": "D",
      "passes": true,
      "notes": "Criado src/routes/_authenticated.tsx como layout pathless com beforeLoad que chama getCurrentUser(). Sem sessão, redireciona para /login com search.redirect validado (sanitizeRedirect previne open redirect: rejeita // e não-/). O retorno de beforeLoad ({ user: AuthenticatedUser }) expande o router context para rotas filhas. Nota técnica: TanStack Router requer pelo menos uma sub-rota filho para pathless layouts (checkRouteFullPathUniqueness conflita com / se sem filhos). Criado dashboard.tsx como placeholder mínimo para permitir geração da route tree — será substituído quando rotas autenticadas reais forem adicionadas. Biome check passa (86 files). Typecheck sem erros novos (erros preexistentes em theme.test.ts e login.tsx são de outras stories). Testes: 229 passed."
    },
    {
      "id": "US-008",
      "title": "Implementar rota pública de login e ação de logout",
      "description": "As a user, I want a login screen and logout action so that I can enter and leave the authenticated area safely.",
      "acceptanceCriteria": [
        "Criar src/routes/login.tsx como rota pública",
        "Tela coleta email e senha e envia para signIn",
        "Em sucesso redireciona para rota autenticada inicial ou redirect válido",
        "Ao acessar /login com sessão válida redireciona para rota autenticada inicial ou redirect válido",
        "Em falha, UI mostra apenas mensagem genérica",
        "Ação de logout chama signOut e invalida estado local",
        "pnpm exec biome check . passes",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 8,
      "group": "D",
      "passes": true,
      "notes": "Criado src/routes/login.tsx com formulário de login (email/senha) usando shadcn/ui (Card, Input, Label, Button). beforeLoad chama getCurrentUser e redireciona para rota autenticada se sessão válida. Login usa signIn server function via useMutation. Em sucesso redireciona para /authenticated ou redirect seguro (validado contra open redirect). Em falha mostra mensagem genérica. Botão 'Limpar sessão' chama signOut. Componentes shadcn input, label, card adicionados. Biome check passa (85 arquivos). Typecheck: único erro pré-existente em tsconfig.json (baseUrl deprecation)."
    },
    {
      "id": "US-009",
      "title": "Criar bootstrap da área autenticada",
      "description": "As an authenticated user, I want an initial protected page so that the app can confirm session state and expose my roles to the UI.",
      "acceptanceCriteria": [
        "Criar src/routes/_authenticated/index.tsx",
        "Página consome usuário autenticado já validado pelo layout protegido",
        "UI consegue ler roles para navegação e experiência",
        "Implementação não usa roles da UI como autorização backend",
        "pnpm exec biome check . passes",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 9,
      "group": "E",
      "passes": true,
      "notes": "Substituído o placeholder dashboard.tsx com a página autenticada completa em src/routes/_authenticated/dashboard.tsx. Nota técnica: não foi possível criar _authenticated/index.tsx porque TanStack Router file-based conflita com src/routes/index.tsx (ambos resolvem para /). A rota /dashboard sob o layout _authenticated serve como página inicial protegida. A página consome user via Route.useRouteContext() do layout pai, exibe perfil (email, username, nickname) e roles como badges informativos. Roles são apenas para exibição — nenhuma decisão de autorização é tomada no cliente. Inclui header com nome do app, ThemeToggle e botão de logout. Biome check passa (86 files). Typecheck sem erros novos (erro pré-existente em theme.test.ts/jsdom)."
    },
    {
      "id": "US-010",
      "title": "Cobrir autenticação com testes unitários e de integração",
      "description": "As a developer, I want automated tests for the auth flow so that regressions in session handling and route protection are caught early.",
      "acceptanceCriteria": [
        "Existem testes para schemas e tipos críticos de autenticação",
        "Existem testes para leitura, escrita e remoção do cookie de sessão",
        "Existem testes para getCurrentUser limpando sessão quando auth:check retorna 401",
        "Existem testes para redirecionamento de rota protegida sem sessão válida",
        "Mocks de ambiente e HTTP não dependem de token persistido no browser",
        "Tests pass",
        "Typecheck passes"
      ],
      "priority": 10,
      "group": "F",
      "passes": true,
      "notes": "Criados testes para auth.schemas (22 tests), auth.types/hasRole+hasSnippet (9 tests), auth-session.server (13 tests: createSession, getSession, removeSession, round-trip), auth-api.server (14 tests: signIn, checkSession, signOut com mocks HTTP), auth.functions (11 tests: getCurrentUser limpando sessão em 401, signIn/signOut server functions com validação CSRF), rota protegida _authenticated (5 tests: sanitizeRedirect, redirect sem sessão). Todos os mocks de ambiente e HTTP são independentes de tokens persistidos no browser. Biome check limpo (92 files). Typecheck sem erros novos (apenas jsdom preexistente em theme.test.ts). 303 tests pass."
    }
  ]
}
